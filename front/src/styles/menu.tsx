import { styled } from "@mui/material/styles";

export const Content = styled("div")(() => ({
  margin: "1.2rem",
}));

export const Title = styled("h1")(() => ({
  display: "flex",
  alignItems: "center",
  marginLeft: 20,
}));
